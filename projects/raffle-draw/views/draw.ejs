<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Winners</title>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <div class="container">
      <div class="btn-group">
        <a href="/tickets" class="btn btn-primary">Ticket list</a>
      </div>
      <h2>Winners</h2>
      <div style="overflow: auto">
        <table class="details-table">
          <thead>
            <tr>
              <th>Position</th>
              <th>Number</th>
              <th>Username</th>
              <th>Price</th>
            </tr>
          </thead>
          <tbody>
            <% for (let i = 0; i < winners.length; i++) { %>
            <tr>
              <td>
                <strong><%= i + 1 %></strong>
              </td>
              <td>
                <strong
                  ><a href="/tickets/t/<%= winners[i].id %>"
                    ><%= winners[i].id %></a
                  ></strong
                >
              </td>
              <td><%= winners[i].username %></td>
              <td class="text-right"><%= winners[i].price %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </body>
</html>
